{% extends 'base.html.twig' %}

{% block title %}New ingredients{% endblock %}

{% block body %}
    <h1>suite ingrédients</h1>
    {{dump(listeEtapes)}}

    {{ include('recette/_form.html.twig') }}
    {# todo : prévoir une vue de validation :  #}
    {# <a href="{{ path('recette_new_etapes',{'id': app.request.get('id')})}}"> passer a l'ajout des étapes</a><br/> #}

    <a href="{{ path('recette_index') }}">back to list</a>
{% endblock %}
