{% extends 'base.html.twig' %}

{% block title %}Commentaires{% endblock %}

{% block body %}
    <h1>Commentaires</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ commentaire.content }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ commentaire.createdAt ? commentaire.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Edited_at</th>
                <td>{{ commentaire.editedAt ? commentaire.editedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('administration_comentaires_index') }}">back to list</a>

    <a href="{{ path('administration_comentaires_edit', {'id': commentaire.id}) }}">edit</a>

    {{ include('administration_comentaires/_delete_form.html.twig') }}
{% endblock %}
