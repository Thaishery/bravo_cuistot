 {# ? Feuille du bouton Delete #ModeAdmin #}

     <form method="post" action="{{ path('administration_comentaires_delete', {'id': commentaire.id}) }}" onsubmit="return confirm('Voulez vraiment supprimer ce commentaire?');">
     
         <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire.id) }}">
     
         <button class="btn col-12 btn-secondary btn-lg px-4">
             Supprimer 
         </button>
     
     </form>
     
