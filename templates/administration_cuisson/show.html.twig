{% extends 'base.html.twig' %}

{% block title %}Cuisson{% endblock %}

{% block body %}
    <h1>Cuisson</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cuisson.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cuisson.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('administration_cuisson_index') }}">back to list</a>

    <a href="{{ path('administration_cuisson_edit', {'id': cuisson.id}) }}">edit</a>

    {{ include('administration_cuisson/_delete_form.html.twig') }}
{% endblock %}
