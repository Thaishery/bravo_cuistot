<form method="post" action="{{ path('administration_cuisson_delete', {'id': cuisson.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cuisson.id) }}">
    <button class="btn">Delete</button>
</form>
