{% extends 'base.html.twig' %}

{% block title %}UniteMesure index{% endblock %}

{% block body %}
    <h1>UniteMesure index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for unite_mesure in unite_mesures %}
            <tr>
                <td>{{ unite_mesure.id }}</td>
                <td>{{ unite_mesure.name }}</td>
                <td>
                    <a href="{{ path('unite_mesure_show', {'id': unite_mesure.id}) }}">show</a>
                    <a href="{{ path('unite_mesure_edit', {'id': unite_mesure.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('unite_mesure_new') }}">Create new</a>
{% endblock %}
