 {% extends 'base.html.twig' %}

     {% block title %}
         Log in!
     {% endblock %}
     
     {% block body %}

         <main class="Form">

             <form method="post">

                 {% if error %}

                     <div class="alert alert-danger">
                         {{ error.messageKey|trans(error.messageData, 'security') }}
                     </div>

                 {% endif %}
             
                 {% if app.user %}

                     <div class="mb-3">
                         Vous etes connecté en tant que {{ app.user.username }}, <a href="{{ path('app_logout') }}"> Se déconnecter </a>
                     </div>

                 {% endif %}
             
                 {# *** Conteneur du titre du formulaire *** #}

                 <article id="bloc_page" class="py-5 text-center col-12">
                 
                     <h1 class="h1 mb-3 font-weight-normal">
                         Formulaire de connection 
                     </h1>

                     <p class="lead"> 
                         Déjà de retour! Merci de vous identifiez 🔏
                     </p>
                 
                     <label for="inputLogin"> Pseudo </label>
                     <input type="text" value="{{ last_username }}" name="login" id="inputLogin" class="form-control" autocomplete="username" required autofocus>
                    
                     <label for="inputPassword"> Mot de passe </label>
                     <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
             
                     <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
             
                     <section class="checkbox mb-3">
                     
                     <hr style="color:rgba(25,135,84,0.9);"/>

                         <label>
                             <input type="checkbox" name="_remember_me">
                             Se souvenir de moi
                         </label>
                   
                     <hr style="color:rgba(25,135,84,0.9);"/>

                     </section>

                     {#
                         Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                         See https://symfony.com/doc/current/security/remember_me.html                 
                     #}

                     <section class="btn-group-vertical col-12">

                         {# *** Bouton: Se Connecter *** #}
                         <button type="submit" class="btn btn-dark btn-lg px-4 gap-3"> 
                            Se connecter 
                         </button>
    
                         {# *** Bouton: S'inscrire *** #}
                         <a type="submit" href="{{ path('app_register') }}" class="btn btn-success btn-lg px-4 gap-3">                                 
                             S'inscrire
                         </a>
                      
                     </section>

                 </article>

             </form>

         <main>

     {% endblock %}

    
