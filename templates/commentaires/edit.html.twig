{# {% extends 'base.html.twig' %} #}

{# {% block title %}Modifier vottre commentaire{% endblock %} #}

{# {% block body %} #}
    <div class="row col-12">
     {{ form_start(form) }}
        <div class="col-2">
        {{form_label (form.content, 'Contenu de votre commentaire:')}}
        </div>
        <div class="col-12">
        {{form_widget (form.content, 
        {
            'attr':
            {
                'class':'col-12'
            }
        })}}
        </div>
         <button class="btn col-12 btn-dark btn-lg px-4 " >
             {{ button_label|default('Sauvegarder') }} 
         </button>
         {# <a href="{{path('recette_show',{'id': id})}}" id="editComment" class="btn btn-success btn-lg">Retour a la recette</a> #}
         <script type="text/javascript" src="{{asset('js/modalCommentaire.js')}}"></script>
     {{ form_end(form) }}
     </div>
{# {% endblock %} #}
