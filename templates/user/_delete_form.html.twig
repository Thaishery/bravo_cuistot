 {# ? Feuille du bouton Delete #ModeAdmin #}

     <form method="post" action="{{ path('user_delete', {'id': user.id}) }}" onsubmit="return confirm('Voulez vraiment supprimer ce fichier?');">
     
         <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
     
         <button class="btn col-12 btn-dark btn-lg px-4">
              Supprimer 
         </button>
     
     </form>
 