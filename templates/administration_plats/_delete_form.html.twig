 {# ? Feuille du bouton Delete #ModeAdmin #}

     <form method="post" action="{{ path('administration_plats_delete', {'id': plat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">

         <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plat.id) }}">

         <button class="btn col-12 btn-dark btn-lg px-4">
             Supprimer 
         </button>

     </form>
