{% extends 'base.html.twig' %}

{% block title %}Plats{% endblock %}

{% block body %}
    <h1>Plats</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plat.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ plat.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('administration_plats_index') }}">back to list</a>

    <a href="{{ path('administration_plats_edit', {'id': plat.id}) }}">edit</a>

    {{ include('administration_plats/_delete_form.html.twig') }}
{% endblock %}
