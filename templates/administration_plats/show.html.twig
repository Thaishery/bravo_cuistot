 {# ? Feuille : Gestion des Plats #ModeAdmin #}  
 
 {% extends 'base.html.twig' %}
     
     {% block metaTitle %}
         " Mode Admin: Plats"
     {% endblock %}

     {% block title %}
         Mode Admin: Plats
     {% endblock %}
     
     {% block body %}

         <main class="Form"> 
     
             <section class="text-center">
 
                 <h1 class="h1 mb-3 font-weight-normal"> Mode Admin: Plats </h1>

             </section>
     
             <article>

                 <table class="table table-dark table-hover">

                     <tbody>

                         <tr>
                             <th>Id</th>
                             <td>{{ plat.id }}</td>
                         </tr>

                         <tr>
                             <th>Name</th>
                             <td>{{ plat.name }}</td>
                         </tr>

                     </tbody>

                 </table>
     
             </article>  

             <article>

                 <button href="{{ path('administration_plats_edit', {'id': plat.id}) }}" type="submit" class="btn btn-success col-12 btn-lg px-4 gap-3">                                
                     Modifier
                 </button>

                 <button href="{{ include('administration_plats/_delete_form.html.twig') }}" type="submit" onsubmit="return confirm('Voulez vraiment supprimer ce fichier?');" class="btn btn-success col-12 btn-lg px-4 gap-3">                                
                     Supprimer
                 </button>

                 <button href="{{ path('administration_plats_index') }}" type="submit" class="btn btn-success col-12 btn-lg px-4 gap-3">                                
                     Retour à la liste 
                 </button>

             </article>

         </main>    

     {% endblock %}
